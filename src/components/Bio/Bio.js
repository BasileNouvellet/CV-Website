// @flow

import * as React from 'react';

import './bio.css';

type PropsType = {|

|};

function Bio(props: PropsType) {

    return (
        <div className="bio-container">
            This is Bio
        </div>
    );
}

export default Bio;
